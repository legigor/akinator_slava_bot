name: Build, Test and Deploy

on: [push]

jobs:

  build:
    name: Build the docker image
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v1
        run: |
          docker-compose build

