name: Build, Test and Deploy

on: [push]

jobs:

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Build the image
      - uses: actions/checkout@v1
        run: |
          docker-compose build

